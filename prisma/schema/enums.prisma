enum Role {
    LANDLORD
    TENANT
    ADMIN
}

enum UserStatus {
    BAN
    UNBAN
}

enum RENTAL_STATUS {
    PENDING
    APPROVED
    REJECTED
}

enum PropertyStatus {
    PENDING
    AVAILABLE
    RENTED
    UNAVAILABLE
}
