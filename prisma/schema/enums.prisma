enum Role {
    LANDLORD
    TENANT
    ADMIN
}

enum UserStatus {
    BAN
    UNBAN
}

enum RentalStatus {
    PENDING
    APPROVED
    REJECTED
}

enum RentalType {
    MONTHLY
    YEARLY
}

enum PropertyStatus {
    PENDING
    AVAILABLE
    RENTED
    UNAVAILABLE
}

enum PaymentStatus {
    PENDING
    SUCCEEDED
    FAILED
    REFUNDED
    CANCELED
}
