enum Role {
    LANDLORD
    TENANT
}

enum UserStatus {
    BAN
    UNBAN
}

enum RENTAL_STATUS {
    PENDING
    APPROVED
    REJECT
}

enum PropertyStatus {
    AVAILABLE
    RENTED
    PENDING
    UNAVAILABLE
}
